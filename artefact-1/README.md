3-tier:
- Frontend (Routing Logic + Forms). e.g. Posts a HTTP Request (GET) to the Logic API.

- Logic (Fetches data from database etc)

- PostgresSQL database (constructed i Compose.yml)

Run by: ``docker compose up``